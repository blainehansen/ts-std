{
	"name": "@ts-actually-safe/monads",
	"version": "0.1.0",
	"description": "Base Maybe and Result types. Used by many other libraries in the ts-actually-safe system.",
	"author": "Blaine Hansen <faichenshing@gmail.com>",
	"homepage": "https://github.com/blainehansen/ts-actually-safe#readme",
	"license": "MIT",
	"main": "dist/index.js",
	"typings": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/blainehansen/ts-actually-safe.git"
	},
	"scripts": {
		"test": "mocha -r ts-node/register lib/**/*.spec.ts",
		"build": "rm -rf dist/* && tsc",
		"preversion": "npm run build && npm test",
		"postversion": "git push origin master && git push origin master --tags && npm publish"
	},
	"bugs": {
		"url": "https://github.com/blainehansen/ts-actually-safe/issues"
	},
	"dependencies": {
		"@ts-actually-safe/types": "^0.1.0"
	}
}
